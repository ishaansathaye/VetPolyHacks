class Event():
    def __init__(self, name, zip, lat, longitude):
        self.name = name
        self.zip = zip
        self.lat = float(lat)
        self.longitude = float(longitude)

    
